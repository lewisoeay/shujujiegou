/*#include<stdio.h>
int main(){
	int a, b, result,count=0,max,min;
	printf("请输入两个数");
	scanf("%d""%d", &a, &b);
	a > b ? max = a, min = b : max = b, min = a;
	if (max - min > 0) {
		max = max - min;
		count++;
	}result = max;
	if (result == 0) {
		printf("两数相除的结果是%d", count);
	}
	else { printf("两数相除的结果是%d.余数是%d", count, result); }
	
	
		 
		
		
		

	
	



	return 0;
}*/