/*#include<stdio.h>
void panduan(int* max, int* array, int length);
int main(void) {
	int a[] = { 2,6,5,4,9,8,7,3 };
	int Max;
	panduan(&Max, a, 8);
	printf("%d", Max);





	return 0;
}void panduan(int* max,  int* array, int length)
{
	*max = array[0];
	
	for (int i = 1; i < length; i++) {
		if (*max < array[i]) {
			*max = array[i];
		}}

}*/